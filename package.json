{
  "name": "shade-shell",
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "lint": "eslint . --fix",
    "types": "npx @ts-for-gir/cli generate -o @girs --ignoreVersionConflicts",
    "build": "meson setup --prefix \"$(pwd)/dist\" build --wipe ; meson install -C build",
    "dev": "cp ./data/*.xml ./dist/share/glib-2.0/schemas && glib_compile_schemas ./dist/share/glib-2.0/schemas && pnpm run build && GSETTINGS_SCHEMA_DIR=\"$(pwd)/dist/share/glib-2.0/schemas\" ./dist/bin/*"
  },
  "devDependencies": {
    "@eslint/js": "latest",
    "@girs/adw-1": "latest",
    "esbuild": "latest",
    "eslint": "latest",
    "typescript": "latest",
    "typescript-eslint": "latest"
  },
  "dependencies": {
    "gnim": "^1.9.1",
    "gnim-schemas": "^0.3.0"
  },
  "prettier": {
    "semi": false
  }
}